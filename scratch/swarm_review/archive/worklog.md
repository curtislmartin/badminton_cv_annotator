# Swarm review worklog — src/annotator + src/scraper

Date started: 2026-08-04. Orchestrator: Claude (Fable 5), skill `swarm-review`.
Working dir: `scratch/swarm_review/`. Report-only run: no edits, no fixes, no commits.

## Scope (locked)

- 30 source modules: `src/annotator/**` and `src/scraper/**`, listed in `scope_files.txt`.
- Excluded: `src/annotator/validation_overlay/` (helper tool, user ruling), all `__init__.py` (trivial).
- Tests: deferred to a follow-up pass, NOT pre-authorised. Report names which test files
  follow from flagged modules.
- No agent (or Claude) reads `.md` files except those under `.claude/` and the skill rubric.
- 12,826 source lines; metrics in `metrics.json` (30/30 parsed, coverage complete).

## User rulings (2026-08-04)

1. No Haiku failure-handling lens ("haiku is too dumb" for silent fails). No dedicated
   silent-fail lens at all: picked up incidentally only, otherwise refactorer's job.
2. Codex replaces BOTH intermediate seats (Sonnet verifier + Opus probe assessor),
   merged into ONE `gpt-5.6-luna` effort=`max` read-only call per module (30 calls).
   Launch raw (`codex exec`, companion caps at xhigh). Luna-at-max is user-approved.
3. Codex is first-class: grant any permissions Claude has, incl. PyCharm where usable.
4. Run codex in batches of ~5 modules; check in with user after each batch (report and
   continue; user monitors usage and can interrupt). RELAXED mid-run: user said run
   through all batches without waiting; stop only for pertinent questions. The Sol
   de-yuck packet still needs explicit user sign-off before launch (ruling 6).
5. Working docs live in `scratch/swarm_review/`; keep this worklog current for resumability.
6. Final stage (user-added, refined): `gpt-5.6-sol` effort=`medium` does the prose
   rewrite of the report outputs itself (write access; /write-clearly + de-yuck
   standards in its packet; packet agreed with user before launch). Claude's ONLY job
   at that stage: save `.bk` copies of the originals first, then diff and correct
   factual inaccuracies Sol introduced. No Claude-side prose editing.

## Pipeline (adapted from skill template)

1. Mechanical pass — DONE. `metrics.json`; 26 seeded findings (mega-module x9,
   mega-function x5, deep-nesting x12); 0 unused-private candidates.
2. Workflow swarm (script `workflow_run.js`, generated by `build_workflow_run.py`):
   - 3 Haiku-low lens scans (naming/structure/style) per chunk; 32 chunks
     (rally_segmentation.py and e2e_court_annotator.py split in two at a top-level def).
   - 1 cold-read probe per module: Haiku low, except Sonnet low for the 2 chunked modules.
   - 3 Sonnet cross-module scans: annotator-core group, calibration group, scraper group.
   - Adaptation vs template: agents Read their own chunk by line range (workflow args
     can't carry 13k source lines); verify/assess stages removed from the workflow
     (codex owns them). Script returns compact statuses only; findings extracted from
     the run journal on disk.
3. Codex stage: per module, one luna-max call = ground-truth read, probe assessment
   (comprehension-trap findings), then conservative claim verification
   (CONFIRMED / MISREAD_BUT_TELLING / KILLED; kill only demonstrable hallucinations).
   Briefs + verdicts in `packets/`. Logs + exit codes in `codex_jobs/`.
4. Orchestrator audit: read each implicated file once, judge survivors + the 26 seeded
   findings, apply rubric exceptions.
5. Report: `scratch/swarm_review/report.md` (skill default local_scratch overridden by
   user ruling). Hit-list also presented in chat.

## Progress

- [x] Scope resolved, rulings collected
- [x] Mechanical pass (`metrics.json`)
- [x] Workflow swarm COMPLETE: runId `wf_7e64ca5f-87e`, 129/129 jobs, 0 failed,
      ~3.87M subagent tokens, 44 min; journal at
      `~/.claude/projects/-home-ariel-Documents-COSC594-badminton-cv-annotator/a6d291df-8e22-44dc-9ef4-079e438210a6/subagents/workflows/wf_7e64ca5f-87e/`
- [x] Journal extracted to packets: 458 lens findings across 96 scan jobs, 30/30
      probes, 28 cross findings (11 annotator-core, 8 calibration, 9 scraper).
      Briefs built 30/30.
- [x] Codex batches 1-6 ALL DONE: 33/33 luna-max calls exit 0, 33/33 verdicts
      parse. Total codex ~2.66M tokens (~2.7% weekly). Funnel totals from
      verdicts: 275 CONFIRMED, 105 KILLED, 28 MISREAD_BUT_TELLING; 58 probe
      findings; 8 incidental notes.
- [x] Orchestrator audit COMPLETE: all 30 modules read and judged; rulings in
      audit_notes.md. 35 hit-list entries, ~55 detail items, ~45 audit kills,
      6 resurrections from luna quote-gate kills.
- [x] Report written: scratch/swarm_review/report.md. Hit-list presented in chat.
- [x] Audit-level transparency added to report (user request 2026-08-04):
      A1/A2/A3 legend, per-entry tags on all 35 hit-list rows, module-by-module
      read-depth accounting in methodology. A3 entries (e2e + rally_seg
      docstring/contract items) rest on luna's ground-truth read + verbatim
      quotes without an orchestrator source read.
- [ ] Sol de-yuck pass on report.md DEFERRED (user: "no de-yuck needed yet").
      Packet spec preserved in ruling 6 if revived.

## Follow-on task (2026-08-04): refactor handoff proposal for Curtis

User rulings: Curtis (not Ariel) executes the refactor, on Codex with the
skills at ~/.codex/skills; the document is standalone-first; none of this
session's rulings bind Curtis; authority boundaries are his to agree with
Ariel. Pipeline: luna-max grounding jobs -> Claude draft -> sol-high
technical audit -> sol-med write-clearly -> sol-med de-yuck (both are Codex
native skills), with .bk factual-drift checks by Claude between passes.

- [x] Grounding briefs written: handoff/brief_run_video.md (stage map of
      run_video()), handoff/brief_rally_seams.md (seam map + entry-8 anchor;
      pyrefly_nav shim copied to local_scratch/).
- [x] Both luna-max jobs launched (workspace-write sandbox, single output
      file each; logs + exit codes in handoff/).
- [x] Draft written: scratch/swarm_review/refactor_handoff_prompt.md, with
      <SUMMARY_RUN_VIDEO> / <SUMMARY_RALLY_SEG> placeholders pending the maps.
- [x] Luna maps integrated (both jobs exit 0). run_video: 16 stages with
      seam signatures; nested horizon loop is the heaviest coupling. rally:
      three-way split NOT clean (sticky bridge 1071-1225 + segment_video
      facade 1283-1367 cross the seams); CLI-only extraction verified clean;
      entry-8 filename anchor confirmed at all three ends.
- [x] Sol high technical audit DONE (exit 0, ~93k tokens, verdict REVISE):
      1 blocker (entry-8 gate wrongly unified the two mask artefacts),
      7 material, 3 minor. All 11 accepted and fixed. Claude verified the two
      new source claims first-hand (FpsConstants is a frozen dataclass at
      fps_constants.py:22; composition_mask.py:166 writes the dead mask).
      Batch table now 9 batches (score_video split out of run_video's batch).
      Provenance headers added to both luna maps.
- [x] Prose passes DONE. First write-clearly launch refused correctly on a
      Claude brief error ("both tables" vs one table); brief fixed,
      relaunched. Write-clearly rewrote throughout; de-yuck made 7 light
      edits. Both passes drift-checked against .bk1/.bk2 (in handoff/):
      full read plus mechanical diff of file-ref and number multisets, all
      empty. Codex usage this task: luna maps 88k + 169k, sol audit 93k,
      write-clearly 96k (+28k refused first run), de-yuck 29k; ~0.5M total.
      DELIVERABLE: scratch/swarm_review/refactor_handoff_prompt.md.
- [x] Correction notes added to report.md (user request): entry 12
      (`FpsConstants._fields` does not exist; dataclass, use
      `dataclasses.fields`) and entry 13 (seam boundaries corrected by the
      luna seam map; three-way split not clean, CLI cut is). Entry 8 was
      confirmed as written, so no note.

## Resumability

- Workflow: resume with `Workflow({scriptPath: "scratch/swarm_review/workflow_run.js",
  resumeFromRunId: <runId below>})`; journal at the run's transcriptDir (path TBD).
- Codex: per-module verdict files `packets/<module>__verdict.json`; a module is done
  iff its verdict file exists and parses. Exit codes in `codex_jobs/<module>.exit`.
- Batches completed and check-ins are logged at the bottom of this file as they happen.

## Codex batch plan (~5 modules each, launched via run_codex_batch.sh <stems>)

Stems are package-qualified: `src/annotator/config.py` -> `annotator_config`,
`src/annotator/calibration/sweep.py` -> `annotator_calibration_sweep`, etc.

1. scraper core: scraper_config scraper_stage1_index scraper_stage2_transcripts
   scraper_stage3_triage scraper_download_scraped_videos
2. scraper rest + small annotator: scraper_stage10_clean scraper_stage11_pairing
   annotator_batch_report annotator_dead_mask annotator_resolve
3. calibration A: annotator_calibration_fixtures annotator_calibration_schemas
   annotator_calibration_run_cli annotator_calibration_selection annotator_calibration_scoring
4. calibration B + annotator config (6 jobs): annotator_calibration_sweep
   annotator_calibration_gt_scoring_a annotator_calibration_gt_scoring_b
   annotator_types annotator_config annotator_fps_constants
5. annotator mid: annotator_composition_mask annotator_doubles_flag annotator_replay_mask
   annotator_inpaint_guard annotator_experiment_records
6. annotator big (7 jobs): annotator_court_evidence annotator_point_winner
   annotator_run_video annotator_e2e_court_annotator_a annotator_e2e_court_annotator_b
   annotator_rally_segmentation_a annotator_rally_segmentation_b

Split seats (user-approved extra luna-max jobs, quota headroom confirmed <1% weekly):
the three heaviest loads run as two calls each — brief _a = probe assessment + claims
below the split line, brief _b = remaining claims; both ground-truth the whole module.
Split lines: e2e 668, rally 749, gt_scoring 527 (score_video def). Audit merges the
_a/_b verdict files per module. 33 codex calls total.

## Run log

- 2026-08-04: setup, metrics done. Workflow launched. Journal result entries carry no
  labels; extractor classifies via agent transcript prompts instead (rewritten).
- Caught pre-run: `annotator/config.py` vs `scraper/config.py` stem collision in
  packets/; fixed by package-qualified stems in extractor + brief builder.
- Swarm complete (129/129). Extraction + 30 briefs OK.
- Batch 1 (scraper core) DONE, all exit 0, 5/5 verdicts parse. Verdict tally:
  29 CONFIRMED, 6 KILLED, 2 MISREAD_BUT_TELLING; 11 probe findings; 2 incidental.
  Codex usage ~356k tokens (56k-88k per module). Batch 2 launched.
- Batch 2 DONE, 5/5 verdicts parse: 35 CONFIRMED, 5 KILLED, 0 MISREAD; 5 probe
  findings; 3 incidental. ~345k tokens. Batch 3 (calibration A) launched.
- Gotcha: an earlier `cd scratch/swarm_review` persisted into later Bash calls and broke
  relative-path validation once; validation commands now use absolute paths
  (`validate_batch.py <stems>` is the reusable checker).
- Batch 3 DONE, 5/5 parse: 35 CONFIRMED, 13 KILLED, 5 MISREAD; 15 probe findings;
  0 incidental. ~373k tokens; scoring.py had the highest kill rate (8 of 21).
  Batch 4 (6 jobs incl. gt_scoring split) launched.
- Batch 4 DONE, 6/6 parse: 67 CONFIRMED, 16 KILLED, 8 MISREAD; 12 probe findings.
  ~568k tokens; sweep.py killed 11 of 26 claims; gt_scoring split worked (a=167k,
  b=82k tokens). User calibration: 1% codex weekly ~= 1M tokens; full run ~2.5%.
  Batch 5 (annotator mid) launched.

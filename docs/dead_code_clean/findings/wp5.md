# WP5 raw return (gpt-5.6-luna, read-only sweep, 2026-08-01)

1. CLUSTER SHAPE — `src/bst_x/pipeline` builds and validates clips and paired artefacts, `preparing_data` extracts and collates pose/shuttle arrays, and `model`/`loss` support BST-X training and inference; documented roots cover these entry points plus the reporting tools [docs/dead_code_clean/audit_plan.md:38-50, src/bst_x/data_pipeline_to_model_train.md:65-101,457-505]. Both heuristics are live through `REGISTRY` and `apply_heuristic.run` [src/bst_x/preparing_data/heuristics/__init__.py:12-18, src/bst_x/preparing_data/apply_heuristic.py:166-235]. No D-unreach or D-prod claim met the root-manifest evidence bar.

2. LEDGER —

WP5-1 | S | src/bst_x/preparing_data/raw_extract.py:build_stem_to_path | `build_stem_to_path` and `build_clip_path_index` implement the same recursive `clip_stem -> .mp4 Path` index, leaving two sources of truth. | evidence: src/bst_x/preparing_data/raw_extract.py:199-201,285; src/bst_x/pipeline/clip_index.py:28-49; src/bst_x/pipeline/data_access.py:341-344; src/bst_x/build_fe_stats_jsons.py:148-155; roots/callers checked: documented `preparing_data.raw_extract`, `pipeline.data_access`, `build_fe_stats_jsons`, `pipeline.clip_index`, full tracked `src/`, `tests/`, and docs | absorb the raw helper into `pipeline.clip_index.build_clip_path_index` and delete the local definition | confidence high

WP5-2 | U | src/bst_x/preparing_data/prepare_train_on_shuttleset.py:_load_clip_npys | `_load_clip_npys` loads every `_failed.npy` boolean payload although `_align_shuttle_and_truncate` consumes only each array’s length. | evidence: src/bst_x/preparing_data/prepare_train_on_shuttleset.py:511-534,537-582,746-752; raw output creation at :348-355; roots/callers checked: documented `preparing_data.prepare_train_on_shuttleset` root and `collate_npy` call chain | replace the failed-array loads with frame-length reads and pass lengths to `_align_shuttle_and_truncate`, deleting the unused boolean payload loads | confidence high

WP5-3 | C | src/bst_x/preparing_data/heuristics/current.py:apply / src/bst_x/preparing_data/prepare_train_on_shuttleset.py:detect_players_2d | `current.apply` deliberately mirrors the live detector’s gate, ordering, and normalisation across a raw-array boundary, making the duplication a byte-identity isolation contract. | evidence: src/bst_x/preparing_data/heuristics/current.py:1-33,51-120; src/bst_x/preparing_data/prepare_train_on_shuttleset.py:122-250; src/bst_x/preparing_data/heuristics/__init__.py:12-18; roots/callers checked: documented `preparing_data.apply_heuristic` and `preparing_data.prepare_train_on_shuttleset` roots, registry dispatch, and heuristic tests | leave separate because unifying them would couple raw replay to the heavy live extractor and remove the parity oracle | confidence high

WP5-4 | C | src/bst_x/preparing_data/shuttleset_dataset.py:create_bones / src/bst_x/preparing_data/augmentations.py:recompute_bones_torch | `create_bones` and `recompute_bones_torch` intentionally keep identical bone semantics in NumPy collation and Torch augmentation. | evidence: src/bst_x/preparing_data/shuttleset_dataset.py:91-103; src/bst_x/preparing_data/augmentations.py:71-82,216-224; tests/test_augmentations.py:106-150,157-205; roots/callers checked: `prepare_train_on_shuttleset.py:409-414`, `bst_x_train.py:37,583-584`, augmentation tests | leave as an isolation mirror because coupling would make offline collation depend on Torch; retain the parity test | confidence high

WP5-5 | C | src/bst_x/model/tempose.py:MultiHeadAttention / src/bst_x/model/bst.py:MultiHeadCrossAttention | The attention implementations share mechanics but have different self-attention versus cross-attention contracts, and the project explicitly declined unification to preserve BST lineage. | evidence: src/bst_x/model/tempose.py:57-101,104-132; src/bst_x/model/bst.py:31-105,108-125,338-363; docs/architecture_notes/completed_general_refactors/simplification_pass/refactor_summary.md:121-122; roots/callers checked: `BST` self-attention and cross-attention call sites plus documented training/inference roots | leave separate without adding a base abstraction; the duplication preserves the two model contracts | confidence high

WP5-6 | S | src/bst_x/hparam_sweep.py:read_run_mean/read_run_per_class_mean | The fixed-five manifest readers duplicate the arithmetic in `cumulative_mean` and `per_class_mean`, leaving parallel metric-reduction bodies. | evidence: src/bst_x/hparam_sweep.py:300-324,355-371; callers at :190-203,536,582,604-605,789; roots/callers checked: documented `hparam_sweep` root and `tests/test_hparam_sweep.py:366-387` | keep the fixed-five validation, route both readers through the existing reducers, and delete the duplicate arithmetic | confidence high

WP5-7 | C | src/bst_x/pipeline/data_access.py:get_clip_records / src/bst_x/preparing_data/prepare_train_on_shuttleset.py:_resolve_clips_and_labels | Both functions read `clips_master.csv`, filter taxonomy/split rows, and derive labels, but they intentionally differ on missing files and returned data contracts. | evidence: src/bst_x/pipeline/data_access.py:274-400; src/bst_x/preparing_data/prepare_train_on_shuttleset.py:427-508; src/bst_x/pipeline/config.py:338-355; roots/callers checked: documented `pipeline.data_access`, `prepare_train_on_shuttleset`, and `collate_npy` roots | leave separate because the query API preserves missing paths as `None`, while collation drops missing files and routes unknown rows to a separate root | confidence high

WP5-8 | C | src/bst_x/run_tracker.py:track_run/track_serial / src/bst_x/aim_backfill.py:backfill_run / src/bst_x/hparam_sweep.py:run_cell / src/bst_x/run_overview.py:_read_manifests / src/bst_x/bst_x_reporting.py:_log_epoch_tb / src/bst_x/result_utils.py:plot_confusion_matrix | The run/reporting surfaces touch adjacent artefacts but have distinct contracts as manifest writer, Aim backfill, sweep controller, manifest aggregator, TensorBoard sink, and result renderer. | evidence: src/bst_x/run_tracker.py:51-154,280-300; src/bst_x/aim_backfill.py:171-214; src/bst_x/hparam_sweep.py:287-297,489-635; src/bst_x/run_overview.py:22-121; src/bst_x/bst_x_reporting.py:52-143; src/bst_x/result_utils.py:27-134; callers at src/bst_x/bst_x_train.py:38,47,63-65,653,717,931,944,1240,1297; roots/callers checked: documented training, overview, Aim, sweep, and result roots | leave the boundaries separate because consolidation would couple optional Aim, TensorBoard, pandas, and CLI paths without a proven shared contract | confidence high

WP5-9 | C | src/bst_x/hparam_sweep.py:run_cell / src/bst_x/collation_runner.py:main | Both runners launch per-serial `bst_x_train` subprocesses and persist resumable state, but only `hparam_sweep` owns augmentation sweeps, kill rules, verdicts, and reference promotion. | evidence: src/bst_x/hparam_sweep.py:454-475,489-635; src/bst_x/collation_runner.py:1-18,39-104; roots/callers checked: documented `hparam_sweep` and `collation_runner` roots and their CLI docstrings | leave the stripped-down runner independent so simple taxonomy/collation sweeps do not carry hparam verdict machinery | confidence high

WP5-10 | U | src/bst_x/aim_backfill.py:_derive_tags | `_derive_tags` accepts and receives `serial_no` but never reads it, so `backfill_run` recomputes identical run-level tags once per serial. | evidence: src/bst_x/aim_backfill.py:71-99,198-204; documented backfill flow at src/bst_x/data_pipeline_to_model_train.md:479-485; roots/callers checked: documented `aim_backfill` root and `backfill_run` loop | remove the unused parameter and compute the base tags once before the serial loop, copying them before adding `best` | confidence high

WP5-11 | U | src/bst_x/bst_x_infer.py:infer | `infer` unpacks `labels` from every loader batch but returns only predictions and never reads the label binding. | evidence: src/bst_x/bst_x_infer.py:52-72,133-134; src/bst_x/data_pipeline_to_model_train.md:489-496; roots/callers checked: documented `bst_x_infer` root, `Task.infer`, and `tests/test_inference_smoke.py:...` | make the third tuple item anonymous (`_`) and remove the misleading local surface while retaining the dataset tuple contract | confidence high

WP5-12 | T | src/bst_x/preparing_data/heuristics/sticky_anchor.py:compatibility aliases | The four compatibility aliases are used by tests and archived scripts, while the live annotator caller uses the public names and the source marks the aliases for Stage 7 removal. | evidence: src/bst_x/preparing_data/heuristics/sticky_anchor.py:151-154,320-321; src/annotator/rally_segmentation.py:1199-1231; tests/test_sticky_anchor.py:28-32,148-460; tests/test_doubles_overcount.py:36,206-207; scripts/archive/m_sticky_gate_arm.py:83-147; archive exclusion: docs/dead_code_clean/audit_plan.md:20-22; roots/callers checked: tracked non-archive production, tests, and documented heuristic roots | migrate tests to the public functions, then delete the four aliases at the stated Stage 7 boundary | confidence high

3. OUTWARD NOTES —

- `src/bst_x/pipeline/court_utils.py:26-180` mirrors `src/shared/court.py`; cross-package ownership is WP2.
- `src/bst_x/pipeline/shuttle_extractor.py:41-46` is called by `src/bric/perception/shuttle.py:96`; compare TrackNet mode handling under WP2.
- `src/bst_x/pipeline/download_videos.py:33-39` is called by `src/scraper/download_scraped_videos.py:499`; cross-package ownership is WP2.
- `src/bst_x/pipeline/player_mapping.py:70-102` and `src/bst_x/pipeline/clip_generator.py:37-76` are consumed by `scripts/build_shots_master.py:111-115`; cross-boundary ownership is WP7.
- `src/bst_x/pipeline/clip_generator.py:88-105` records an unverified frame-offset relationship with `src/bric/preprocessing/slice_rallies.py:96`; cross-package correctness review belongs to WP2.
- `src/bst_x/result_utils.py:98-134` is reused by `src/bric/eval.py:187`; the cross-package consumer is outside WP5.
- `src/bst_x/preparing_data/heuristics/sticky_anchor.py:89-279` is consumed by `src/annotator/rally_segmentation.py:1199-1231`; public API ownership is outside WP5.
- `src/bst_x/run_tracker.py:167-179` is used by `src/bric/train.py:363`; shared metadata ownership is outside WP5.

4. NOT CHECKED — `src/bst_x/TrackNetV3/` and `src/bst_x/validation_scripts/` were excluded by assignment; cross-package implementations were recorded only as outward notes; `data/`, `experiments/`, and credentials were not examined. Full pytest and whole-project Pyrefly checks were not run for this read-only audit. Ruff passed with exit code 0 using the no-cache scoped command.

# WP3 raw return (gpt-5.6-luna, read-only sweep, 2026-08-01)

1. CLUSTER SHAPE — The cluster runs the annotator chain from fps/config resolution through rally segmentation, masks, doubles detection, court evidence, landing and winner attribution (`src/annotator/run_video.py:199-620`, `src/annotator/rally_segmentation.py:1283-1420`), with calibration and validation-overlay CLIs as live roots (`src/annotator/calibration/gt_scoring.py:782-810`, `src/annotator/validation_overlay/overlays/shuttle_track.py:99-134`). CourtKeyNet supplies detector batches, scene quads and consensus repair to the annotator (`src/annotator/court_evidence.py:203-237,608-709`, `src/annotator/e2e_court_annotator.py:932-946`).

2. LEDGER

WP3-1 | S | src/annotator/rally_segmentation.py:_find_rally_spans | `_find_rally_spans` recomputes the same threshold, fast-run and long-rest scaffolding already returned by `_rally_regions`, leaving only the default burst-opening choice as its real distinction. | evidence: src/annotator/rally_segmentation.py:489-517,520-563,566-591,1283-1311; callers src/annotator/run_video.py:360-410 and tests/test_scraper_stage8.py:554-589 | absorb the existing `_rally_regions` result into `_find_rally_spans` and delete its repeated setup | confidence high

WP3-2 | S | src/annotator/point_winner.py:inout_verdict | `inout_verdict` repeats the signed inside/outside boundary distance that `landing_margins` already computes for the non-negative margins used by its callers. | evidence: src/annotator/point_winner.py:578-640,761-790,821-849; tests/test_point_winner.py:422-450 | make `inout_verdict` classify the existing `landing_margins` result and delete the repeated geometry calculations | confidence med

WP3-3 | U | src/annotator/config.py:BEST_CONFIG_THRESHOLDS | `BEST_CONFIG_THRESHOLDS` is a stale alias because no production parser or caller selects it and only the stage-8 test uses the name. | evidence: src/annotator/config.py:82-85; src/annotator/rally_segmentation.py:1490-1509; tests/test_scraper_stage8.py:351-356; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete the alias and update the test and stale documentation to use `SHIPPED_THRESHOLDS` | confidence high

WP3-4 | U | src/annotator/config.py:COURT_ABSENT_WINDOW | `COURT_ABSENT_WINDOW` is a stale fixed-25-fps config knob because the live signal resolves `court_absent_window` from the passed fps instead. | evidence: src/annotator/config.py:90; src/annotator/replay_mask.py:52-74,224-243; tests/test_scraper_stage9.py:15,43; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete the alias and update the test and docstring to use `scale_for_fps(fps).court_absent_window` | confidence high

WP3-5 | U | src/annotator/point_winner.py:SUSTAINED_LOSS_FRAMES, MIN_DESCEND_SAMPLES | `SUSTAINED_LOSS_FRAMES` and `MIN_DESCEND_SAMPLES` are stale fixed-25-fps aliases because production landing code receives the corresponding resolved fields from `FpsConstants`. | evidence: src/annotator/point_winner.py:95-99,709-752; src/annotator/run_video.py:539-567; tests/test_point_winner.py:130-145,343-393; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete both aliases and update tests to use resolved `FpsConstants` values | confidence high

WP3-6 | T | src/annotator/court_evidence.py:build_detected_court_inputs | `build_detected_court_inputs` is a test-only thin wrapper that discards the real `case_id` and `parent` before returning `.inputs` from the live full builder. | evidence: src/annotator/court_evidence.py:569-589; production caller src/annotator/e2e_court_annotator.py:942-946; only test caller tests/test_court_evidence.py:217-238; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete the wrapper and make the test use `build_detected_court_evidence(...).inputs` | confidence high

WP3-7 | T | src/annotator/rally_segmentation.py:detect_contacts | `detect_contacts` is a test-only projection of live `detect_contact_flags` that discards impulse values, while production `assemble_contacts` calls the flagged form. | evidence: src/annotator/rally_segmentation.py:1044-1066,1314-1335; only callers tests/test_scraper_stage8.py:17-42,173-176; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete the wrapper and update the test to use `detect_contact_flags` | confidence high

WP3-8 | T | src/annotator/validation_overlay/core/decode.py:fetch_span | `fetch_span` is only exercised by validation-overlay tests, while live rendering streams frames through `iter_span_frames` directly. | evidence: src/annotator/validation_overlay/core/decode.py:263-315; src/annotator/validation_overlay/core/cli.py:246-283; only callers tests/test_validation_overlay_assembly.py:12,33 and tests/test_validation_overlay_core.py:16,120-195; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete the bulk adapter and update tests to stack `iter_span_frames` results | confidence high

WP3-9 | D-prod | src/annotator/calibration/sweep.py:load_winner_config | `load_winner_config` and its private `_load_winner_document` parser have no production caller because the runnable sweep path calls `load_boundary_winner` instead. | evidence: src/annotator/calibration/sweep.py:570-717,730-762; only callers tests/test_sweep.py:309-345; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete the legacy full-winner loader and private parser, retaining `load_boundary_winner` | confidence high

WP3-10 | D-prod | src/annotator/validation_overlay/core/cli.py:RenderPlan.frames | `RenderPlan.frames` is an unread test-inspection property, while callers and tests use `TimelinePlan.frames` through `plan.timeline`. | evidence: src/annotator/validation_overlay/core/cli.py:107-127,153-209,239-343; tests/test_validation_overlay_core.py:149-227; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete the unused property | confidence high

WP3-11 | T | src/annotator/calibration/gt_scoring.py:assert_floors | `assert_floors` is a calibration test helper only because `gt_scoring.main` renders and prints scores without invoking it. | evidence: src/annotator/calibration/gt_scoring.py:690-702,782-810; only caller tests/test_annotator_scoring.py:7-19; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | move the floor assertion into the test and delete the production helper | confidence high

WP3-12 | T | src/annotator/point_winner.py:pick_landing | `pick_landing` is only called by tests, while production uses `pick_landing_to_end` with an event-aware endpoint and mask. | evidence: src/annotator/point_winner.py:709-758; src/annotator/run_video.py:539-567,607-611; only callers tests/test_fps_constants.py:21,262-271 and tests/test_point_winner.py:34,578-607; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete the wrapper and update tests to call `landing_window` and `pick_landing_to_end` | confidence med

WP3-13 | T | src/courtkeynet/wrapper.py:CourtKeyNetDetector.detect | `CourtKeyNetDetector.detect` is only used by wrapper tests because annotator and validation production paths use `detect_batch`. | evidence: src/courtkeynet/wrapper.py:241-312; src/annotator/court_evidence.py:218-234; src/courtkeynet/validation_scripts/render_court_overlay.py:218-231; only callers tests/test_courtkeynet_wrapper.py:210-263; roots checked: CI, Docker FastAPI, tracked documented `-m` roots, runnable `__main__` blocks, conftest/tests, pre-commit, dynamic dispatch | delete the single-frame wrapper and update tests to call `detect_batch([frame])[0]` | confidence med

WP3-14 | C | src/annotator/court_evidence.py:build_static_court_evidence vs build_detected_court_evidence | `build_static_court_evidence` and `build_detected_court_evidence` intentionally mirror the `CourtEvidenceResult` contract while deriving geometry and votes from different parent evidence. | evidence: src/annotator/court_evidence.py:1-5,522-709; callers src/annotator/e2e_court_annotator.py:932-946 | leave as an isolation mirror because merging the builders would couple the static homography prior to CourtKeyNet scene geometry and consensus | confidence high

WP3-15 | C | src/annotator/dead_mask.py:build_dead_mask | `build_composition_mask` and `combine_mask` both produce frame-aligned boolean dead masks, but `build_dead_mask` deliberately selects them as separate COMPOSITION and REPLAY policies. | evidence: src/annotator/composition_mask.py:94-130; src/annotator/replay_mask.py:1-16,224-243; src/annotator/dead_mask.py:45-84 | leave as configured alternatives because merging them would couple cut and court-vote evidence to replay, homography and shuttle evidence | confidence high

WP3-16 | C | src/annotator/rally_segmentation.py:_rolling_mean vs _nan_rolling_mean | `_rolling_mean` and `_nan_rolling_mean` implement the same windowed mean with different missing-data semantics selected by `SmoothingMode`. | evidence: src/annotator/rally_segmentation.py:126-155,953-979; src/annotator/types.py:52-57 | leave as explicit mode implementations because merging them would hide or change zero-fill versus ignore-invisible behaviour | confidence high

WP3-17 | O | src/annotator/run_video.py:run_video | `run_video` repeats the same resolved span-finder options in three direct branches and two segmenter branches, so a new segmentation knob requires synchronised edits and can silently drift across modes. | evidence: src/annotator/run_video.py:199-241,350-417,440-451; callers src/annotator/calibration/gt_scoring.py:718, src/annotator/calibration/sweep.py:396, src/annotator/e2e_court_annotator.py:975, src/annotator/rally_segmentation.py:1577 | hoist the already-resolved span options into one local mapping passed through the existing calls and delete the repeated option lists without changing branch-specific mask logic | confidence med

3. OUTWARD NOTES

- Possible cross-package court-geometry mirror: `src/shared/court.py:14-18,71-138` and `src/bst_x/pipeline/court_utils.py:11-180`, with `src/annotator/court_evidence.py:19,243-388` consuming the shared variant.
- Possible sticky-anchor boundary: `src/annotator/rally_segmentation.py:84-85,1175-1280` and `src/bst_x/preparing_data/heuristics/sticky_anchor.py:89-124,334-335`.
- Cross-stage replay-mask contract to inspect: `src/annotator/replay_mask.py:246-266` and `src/scraper/stage11_pairing.py:150-185`.

4. NOT CHECKED

- `src/courtkeynet/_vendor/` and `src/courtkeynet/validation_scripts/` were excluded by the assignment.
- `docs/**/*.py`, `scripts/archive/`, data, experiments and untracked callers were excluded by the audit plan.
- Full pytest, whole-project pyrefly checking and runtime CourtKeyNet or ffmpeg tests were not run; the owned-tree ruff check passed with exit code 0.
- PyCharm call hierarchy did not resolve the candidate Python callables, so Pyrefly references and tracked `git grep` supplemented the semantic search.

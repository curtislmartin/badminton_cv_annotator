# WP2 raw return (automated read-only sweep, 2026-08-01)

## 1. CLUSTER SHAPE

This cluster covers parallel BST, scraper and BRIC video-processing lanes: BST download-to-dataset entry points (`src/bst_x/pipeline/build_dataset.py:149-258`), scraper stage/download entry points (`src/scraper/__init__.py:1-6`, `src/scraper/download_scraped_videos.py:543-578`), BRIC rally slicing and shuttle extraction (`src/bric/preprocessing/slice_rallies.py:116-220`, `src/bric/preprocessing/extract_shuttle.py:84-217`), and court/player/taxonomy utilities consumed by BST, BRIC and annotator code (`src/shared/README.md:11-18`, `src/courtkeynet/court_corners.py:944-1061`). `shared.video_io` and `shared.temporal` are shared helpers without a matching BST utility shown in the tracked references (`src/shared/video_io.py:16-157`, `src/shared/temporal.py:26-142`, `tests/test_temporal.py:1-14`).

## 2. LEDGER

WP2-1 | P | `src/bst_x/pipeline/download_videos.py::download_all_videos` ↔ `src/scraper/download_scraped_videos.py::download_all_videos` | Both consume URL, video ID and title rows, run parallel yt-dlp downloads with the same retry, throttle, timeout and skip-existing behaviour, but they produce different filename, selection and result contracts with ShuttleSet resolution output on one side and audio-gated TOML manifest output on the other. | evidence: `src/bst_x/pipeline/download_videos.py:41-165,168-238,244-266` matches CSV-to-video and resolution side effects; `src/scraper/download_scraped_videos.py:75-116,262-426,458-540,543-578` adds audio probing, manifest state and `DownloadOutcome`; callers and roots checked: `src/bst_x/pipeline/build_dataset.py:183-197`, `docs/bst_x/data_pipeline_to_model_train.md:122`, `src/scraper/download_scraped_videos.py:562`, `tests/test_scraper_download_videos.py:12-658`, documented pipeline and scraper roots in `docs/archive/dead_code_clean/audit_plan.md` | leave both: the matched worker shape is parallel infrastructure for different package contracts, so deleting either would remove a live lane | confidence high

WP2-2 | C | `src/bst_x/pipeline/court_utils.py::{get_H,get_corner_camera,convert_homogeneous,scale_pos_by_resolution,project,get_court_info,to_court_coordinate,normalize_position,check_pos_in_court}` ↔ `src/shared/court.py::{get_H,get_corner_camera,convert_homogeneous,scale_pos_by_resolution,project,get_court_info,to_court_coordinate,normalize_position,check_pos_in_court}` ↔ `src/courtkeynet/court_corners.py::pick_scene_corners` | The BST and shared court functions have matched homography inputs, normalised coordinate outputs and court-boundary invariants, while CourtKeyNet instead consumes frames and detections and returns native-pixel `CourtQuad` objects with provenance and diagnostics, so equivalence across all three is unproven and the shared mirror deliberately prevents BRIC and annotator code from depending on BST internals. | evidence: `src/bst_x/pipeline/court_utils.py:14-167` and `src/shared/court.py:28-146` show the mirrored pure maths; `src/shared/court.py:17-20` defines local resolution to avoid pipeline imports; `src/shared/README.md:25-33` states the coupling and drift trade-off; `src/courtkeynet/court_corners.py:197-210,944-980` defines the different `CourtQuad` contract; callers and roots checked: `src/bric/preprocessing/preprocess_videos.py:71-77`, `src/bric/dataset.py:17-23`, `src/api/bric_inference.py:40-45`, `src/bst_x/preparing_data/prepare_train_on_shuttleset.py:66`, `src/annotator/court_evidence.py:18-19,229,608,655`, `src/courtkeynet/__init__.py:16-22`, documented API, BRIC, BST and annotator roots | leave the isolation mirrors and CourtKeyNet path; merging them would couple package boundaries and discard distinct pixel-space and provenance contracts | confidence high

WP2-3 | C | `src/bst_x/pipeline/player_mapping.py::{map_players,find_set3_switch_rally,collect_shots}` ↔ `src/shared/player_mapping.py::{map_players,find_set3_switch_rally,collect_shots}` | Both implementations accept the same player and set metadata, return the same Top/Bottom-mapped shot columns and preserve the same set-three switch invariant, but the shared copy deliberately keeps BRIC self-contained rather than importing BST. | evidence: `src/bst_x/pipeline/player_mapping.py:22-129` and `src/shared/player_mapping.py:28-114` match the mapping, retirement guard, set split and CSV-read logic; `src/bst_x/pipeline/player_mapping.py:1-9` and `src/shared/player_mapping.py:1-13` describe the same contract; `src/shared/README.md:11-18,25-33` documents the mirror rationale; callers and roots checked: `src/bst_x/pipeline/clip_generator.py:25,271`, `scripts/build_shots_master.py:10-14,52-64`, `tests/test_player_mapping.py:1-29`, documented BST, BRIC and script roots | leave both copies because deleting the shared copy would make BRIC depend on BST internals, while deleting the BST copy would cross the same boundary in reverse | confidence high

WP2-4 | C | `src/bst_x/pipeline/config.py::parse_flaw_records` and `src/bst_x/pipeline/clip_generator.py::{compute_temporal_bounds,_compute_clip_bounds}` ↔ `src/shared/dataset.py::{parse_flaw_records,compute_temporal_bounds,compute_clip_bounds}` | The functions match on flaw-record, stroke-table and clip-bound inputs and outputs, but the shared copy has a deliberate package-local path contract and differs on negative clip-start clamping, so the two copies are not behaviourally equivalent. | evidence: `src/bst_x/pipeline/config.py:266-293` and `src/shared/dataset.py:86-139` match flaw parsing; `src/bst_x/pipeline/clip_generator.py:37-76,113-143` and `src/shared/dataset.py:217-277` match temporal and clip calculations; `src/shared/dataset.py:260-277` explicitly mirrors the logic but omits BST’s `max(0,start_f)` clamp; `src/shared/README.md:11-18,25-33` documents the package split; callers and roots checked: `src/bst_x/pipeline/build_dataset.py:202-210`, `src/bric/preprocessing/preprocess_videos.py:23-25`, `src/bric/dataset.py:17-23`, documented BST and BRIC roots | leave the mirrors and record the clamp divergence as a contract decision; absorbing one side would silently change clip boundaries for its callers | confidence high

WP2-5 | C | `src/bst_x/pipeline/config.py::Taxonomy` and `TAXONOMIES` ↔ `src/shared/taxonomy.py::Taxonomy` and `TAXONOMIES` | Both packages define taxonomy names, class lists and merge maps, but their registry names, dataclass fields, defaults and `driven_flight` mappings differ, with pipeline names canonical for BST and shared names canonical for BRIC. | evidence: `src/bst_x/pipeline/config.py:33-58,91-107,145-180,205-259` defines BST taxonomy contracts including `bst_25`, `une_v1_14` and `driven_flight -> drive`; `src/shared/taxonomy.py:1-23,63-90,117-177,180-235` defines BRIC contracts including `merged_25`, `une_merge_v1_nosides` and a legacy `driven_flight -> unknown` map; callers and roots checked: `src/bric/network.py:13`, `src/bric/train.py:33`, `src/api/bric_inference.py:38`, `src/bst_x/pipeline/config.py:1-5`, documented BST and BRIC training/API roots | leave the split until the taxonomy wire contracts are intentionally reconciled; replacing either registry would change persisted names or class semantics for its callers | confidence high

WP2-6 | C | `src/bst_x/pipeline/shuttle_extractor.py::{extract_all_shuttles,shuttle_csvs_to_npy,_tracknet_eval_mode}` ↔ `src/bric/perception/shuttle.py::{extract_shuttle,_tracknet_eval_mode}` and `src/bric/preprocessing/extract_shuttle.py::process_one_vid` | Both lanes invoke TrackNet and consume or produce `Frame`, `X`, `Y` and `Visibility` data, but BST performs clip-level batch extraction into flat normalised NPY files whereas BRIC performs rally-level extraction into dense per-video NPZ caches with source-frame rekeying. | evidence: `src/bst_x/pipeline/shuttle_extractor.py:41-46,74-196,201-277` defines batch clip inputs and NPY outputs; `src/bric/perception/shuttle.py:1-19,40-107` defines the vendor subprocess wrapper; `src/bric/preprocessing/extract_shuttle.py:1-30,84-172` defines rally rekeying and dense NPZ output; `src/bst_x/pipeline/build_dataset.py:245-258`, `src/bric/preprocessing/preprocess_videos.py:23-25`, `docs/archive/dead_code_clean/audit_plan.md` documented BST and BRIC roots checked | leave the duplicated TrackNet mode helper and separate extraction lanes; coupling them would break the distinct vendor, cache-granularity and memory contracts | confidence high

WP2-7 | S | `src/shared/eval_plots.py::plot_confusion_matrix` ↔ `scripts/plots/confusion_matrix.py::{annotate_cells,render_panel,main}` | Both take prediction arrays and class labels and render the same F1-sorted dual confusion-matrix panels with zero-safe precision and recall normalisation, while the script additionally owns NPZ loading and CLI output options. | evidence: `src/shared/eval_plots.py:22-137` and `scripts/plots/confusion_matrix.py:41-160` match cell annotation, panel rendering, F1 sorting and safe division; `src/bric/eval.py:17,187-191` is the shared caller; `scripts/plots/confusion_matrix.py:93-112,133-156` owns the NPZ and CLI boundary; roots/callers checked: BRIC evaluation root, documented plotting script root, CI pytest root | absorb the script’s renderer into `src/shared/eval_plots.py:68-137` by adding optional output path and figure-size parameters; retain only NPZ loading and CLI argument handling in `scripts/plots/confusion_matrix.py:93-160`, have its caller pass those options, and delete the duplicate `annotate_cells` and `render_panel` code | confidence high

WP2-8 | C | `src/shared/eval_plots.py::plot_confusion_matrix` ↔ `src/bst_x/result_utils.py::plot_confusion_matrix` | Both accept prediction arrays and produce confusion matrices, but shared plotting uses named classes, F1 sorting and zero-safe division whereas BST preserves numeric class order and its legacy F1 display and unguarded division behaviour. | evidence: `src/shared/eval_plots.py:68-137` versus `src/bst_x/result_utils.py:63-135` show the differing inputs, ordering and normalisation; BST caller `src/bst_x/bst_x_train.py:38,943-951`; BRIC caller `src/bric/eval.py:17,187-191`; documented BST training and BRIC evaluation roots checked | leave the package-local reporting functions until the output and failure semantics are explicitly aligned; absorbing one would alter existing evaluation figures or zero-class behaviour | confidence high

WP2-9 | C | `src/bst_x/pipeline/download_videos.py::build_resolution_csv` ↔ `src/scraper/stage11_pairing.py::build_video_fps_csv` | Both scan a video directory with OpenCV and write one metadata row per video, but BST writes integer ID, width and height with expected-ID and missing-report side effects while scraper writes filename-stem ID and FPS for pairing spans. | evidence: `src/bst_x/pipeline/download_videos.py:168-238` defines resolution CSV columns and missing-ID report; `src/scraper/stage11_pairing.py:43-85,301-327` defines FPS CSV columns and pairing use; BST caller `src/bst_x/pipeline/build_dataset.py:192-197`; scraper caller `src/scraper/stage11_pairing.py:327`; documented BST and scraper roots checked | leave both metadata builders because their sidecar schemas and consumers differ; sharing the scanner would require changing both contracts without reducing a safe duplicate | confidence high

WP2-10 | C | `src/bst_x/pipeline/clip_generator.py::{generate_all_clips,_write_clips_for_video}` ↔ `src/bric/preprocessing/slice_rallies.py::{slice_rally,process_one_vid}` | Both read stroke or rally timing and create video clips, but BST emits per-shot labelled MoviePy clips whereas BRIC emits per-rally ffmpeg clips with source-frame validation, no audio and a different cache layout. | evidence: `src/bst_x/pipeline/clip_generator.py:1-9,113-208,239-299` defines per-shot outputs and MoviePy timing; `src/bric/preprocessing/slice_rallies.py:1-28,87-178,191-220` defines per-rally ffmpeg outputs and frame-count checks; `src/bric/preprocessing/extract_shuttle.py:1-4` consumes the BRIC rally output; `src/bst_x/pipeline/build_dataset.py:202-210` consumes the BST output; documented BST and BRIC roots checked | leave both clip stages because granularity, codecs, labels and downstream cache contracts are intentionally different | confidence high

WP2-11 | C | `src/bst_x/pipeline/download_videos.py::download_video` ↔ `src/scraper/download_scraped_videos.py::_download_one` and `src/scraper/config.py::{SLEEP_INTERVAL_S,MAX_SLEEP_INTERVAL_S,SLEEP_REQUESTS_S,LIMIT_RATE,CONCURRENT_FRAGMENTS}` | Both downloaders hard-code the same yt-dlp retry, sleep, rate, fragment and timeout values, but the BST file explicitly records package isolation and the scraper downloader adds audio verification and manifest handling. | evidence: `src/bst_x/pipeline/download_videos.py:64-86` and `src/scraper/download_scraped_videos.py:315-335` contain the repeated command values; `src/bst_x/pipeline/download_videos.py:73-75` states the deliberate duplication; `src/scraper/config.py:177-188,223-244` is the scraper configuration home; internal entry-point rules are documented in `src/scraper/__init__.py:1-6` and `src/bst_x/pipeline/download_videos.py:1-8`; BST and scraper download callers checked above | leave the constants at the current package boundary until the entry-point contract changes; importing scraper configuration into BST would couple the isolated lanes, and creating another shared owner would add machinery rather than reduce the existing contract split | confidence high

## 3. OUTWARD NOTES

- The API inference triplet is excluded from WP2 and should be compared by WP6: `src/api/inference.py:1-226`, `src/api/bric_inference.py:1-572`, `src/api/bst_x_inference.py:1-272`.

- The BRIC dataset versus shared dataset comparison is excluded from WP2 and should be handled by WP6: `src/bric/dataset.py:1-250`, `src/shared/dataset.py:1-277`.

- Shared temporal helper coverage may need an internal ownership decision because tracked production callers were not found beyond tests: `src/shared/temporal.py:26-142`, `tests/test_temporal.py:1-14`.

## 4. NOT CHECKED

- No files were edited or created.

- No runtime tests, lint or type checks were run because this was a read-only comparison audit.

- `scripts/archive`, `docs/**/*.py`, experiments, generated data and vendored TrackNet internals were outside the audit scope.

- `.env` and credential files were not read.



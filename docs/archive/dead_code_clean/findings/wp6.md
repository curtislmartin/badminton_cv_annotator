# WP6 raw return (automated read-only sweep, 2026-08-01)

CLUSTER SHAPE: `src/bric` provides BRIC training, cache assembly, perception and preprocessing, with documented entry points in `bric.train`, `bric.smoke_test` and the three preprocessing modules (`src/bric/README.md:9-29`, `src/bric/train.py:738-744`, `src/bric/preprocessing/slice_rallies.py:230-231`, `src/bric/preprocessing/extract_shuttle.py:216-217`, `src/bric/preprocessing/preprocess_videos.py:568-569`). `src/api` is live through the Docker `uvicorn src.api.main:app` root, including upload, library, job, model and registry routes (`docs/archive/dead_code_clean/audit_plan.md:21-31`, `src/api/main.py:185-197`, `src/api/main.py:365-572`), with `_process_video` dispatching to BST, BRIC or the fallback stub (`src/api/main.py:208-301`).

LEDGER:

WP6-1 | D-unreach | `src/bric/perception/players.py:detect_and_track` | The ByteTrack-based `detect_and_track` function and its `PlayerTrack` helper chain have no production caller, while live preprocessing and API inference use the frame-detection path instead. | evidence: `src/bric/perception/players.py:55-149,157-266`; live callers `src/bric/preprocessing/preprocess_videos.py:33-37,129-223,267-388` and `src/api/bric_inference.py:33-37,297-350`; Pyrefly refs on `players.py:84:5` found only the definition and git grep found no caller; roots checked: CI pytest/scripts, Docker uvicorn routes, documented `python -m` roots, undocumented `__main__` modules, tests/conftest, hooks, dynamic getattr/importlib/registry/`__all__` | delete `PlayerTrack`, `detect_and_track` and its private helper chain, retaining `DEFAULT_YOLO_WEIGHTS` because `preprocess_videos.py:70` and `api/bric_inference.py:36` use it | confidence high

WP6-2 | U | `src/bric/dataset.py:ShuttleSetDataset.__getitem__` | Disabled modality variants still require both modality caches and build both disabled lane tensors even though `_forward_for_variant` drops them. | evidence: `src/bric/dataset.py:95-111,158-193,340-400`; variant flags `src/bric/train.py:41-45,144-153,451-460`; network lane gates `src/bric/network.py:268-329`; roots checked: CI pytest/scripts, Docker uvicorn routes, documented `python -m` roots, undocumented `__main__` modules, tests/conftest, hooks, dynamic dispatch | align `require_court` and `require_shuttle_cache` with each variant and stop constructing disabled lanes, retaining only fields passed to the selected network | confidence high

WP6-3 | U | `src/bric/preprocessing/preprocess_videos.py:process_one_vid` | The documented per-stroke RGB cache repair path is bypassed on ordinary reruns because an existing player cache returns before `process_rally` can inspect missing RGB tensors. | evidence: cache documentation `src/bric/preprocessing/preprocess_videos.py:38-39`; per-stroke skip `src/bric/preprocessing/preprocess_videos.py:365-387`; early return `src/bric/preprocessing/preprocess_videos.py:433-443`; CLI force policy `src/bric/preprocessing/preprocess_videos.py:521-529`; roots checked: documented `python -m bric.preprocessing.preprocess_videos`, CI scripts, tests and dynamic dispatch | delete the misleading per-stroke existence branch and document the player-cache plus `--force` policy as the sole cache policy | confidence high

WP6-4 | U | `src/bric/preprocessing/preprocess_videos.py:write_player_tracks_cache` | Current tracked consumers do not read the player-cache metadata/confidence fields or the shuttle-cache `frame` array, making those fields write-only storage. | evidence: writer fields `src/bric/preprocessing/preprocess_videos.py:394-429,479-485`; player readers use width/height, bbox and valid only at `src/bric/dataset.py:195-240,274-335`; shuttle writer and reader are `src/bric/preprocessing/extract_shuttle.py:160-165` and `src/bric/diagnostics/evaluate_shuttle.py:51-63`; tracked git grep read sweep found no reads of `vid`, `video_path`, `n_frames`, `fps`, `top_conf`, `bottom_conf` or `frame`; roots checked: CI pytest/scripts, Docker routes, documented `python -m` roots, undocumented `__main__` modules, tests/conftest, hooks, dynamic dispatch | delete write-only `vid`, `video_path`, `n_frames`, `fps`, `top_conf`, `bottom_conf` and dense `frame` fields, retaining width/height, bbox/valid and x/y/visibility | confidence high

WP6-5 | U | `src/api/main.py:_process_video` | The API validates, stores and threads model identifiers, but the fallback ignores them and always reads the first registry model while library dispatch does not branch on `model_id`. | evidence: model discovery and validation `src/api/main.py:319-323,391-392,559-572`; job storage `src/api/jobs.py:15-30,38-59` and `src/api/main.py:450-458,491-508`; first-model selection and ignored arguments `src/api/inference.py:33-50,177-208`; dispatch `src/api/main.py:222-301`; roots checked: Docker routes, tests, registry dispatch and dynamic imports | remove ignored model-selection plumbing and fallback validation until a real dispatcher exists, retaining `/api/models` only for discovery | confidence high

WP6-6 | U | `src/api/registry.py:_summary_live` | `_summary_live` is unused live-code surface because registry list and detail routes serve cached or ground-truth summaries instead. | evidence: unused declaration `src/api/registry.py:306-318`; summary selection `src/api/registry.py:358-374,399-434`; cached detail response and explicit live-pass exclusion `src/api/registry.py:455-509`; Pyrefly refs on `registry.py:306:5` found only the definition and git grep found no caller; roots checked: Docker routes, CI, tests, registry dynamic dispatch and documented roots | delete `_summary_live` and its stale Tier 3 retention comment | confidence high

WP6-7 | U | `src/api/registry.py:_live_splits` and `src/api/bst_x_inference.py:is_available` | The API retains liveness probes with no production caller because registry status uses manifests and sidecars while library dispatch calls `predict` directly. | evidence: `_live_splits` `src/api/registry.py:75-85`; `is_available` `src/api/bst_x_inference.py:160-167`; registry status logic `src/api/registry.py:159-240`; direct library dispatch `src/api/main.py:208-301`; Pyrefly refs on `bst_x_inference.py:160:5` found only the definition and git grep found no production caller; roots checked: CI pytest/scripts, Docker routes, documented `python -m` roots, undocumented `__main__` modules, tests/conftest, hooks, dynamic dispatch | delete both probes and their retained future-work comments | confidence high

WP6-8 | T | `src/api/bst_x_inference.py:available_splits` | `available_splits` is a test-only production symbol used by two direct tests and otherwise only by the dead `_live_splits` wrapper. | evidence: definition `src/api/bst_x_inference.py:170-180`; sole production wrapper `src/api/registry.py:75-85`; direct tests `tests/test_api.py:125-136`; roots checked: Docker routes, CI pytest, documented roots, undocumented `__main__` modules, hooks and dynamic dispatch | classify as test-only and remove the helper with its two direct tests if no public liveness contract is intended | confidence high

WP6-9 | S | `src/bric/eval.py:_select_device` | `bric.eval._select_device` exactly duplicates `bric.train._select_device`, and `eval.py` already imports private training helpers. | evidence: duplicate definitions `src/bric/eval.py:32-37` and `src/bric/train.py:326-331`; eval call `src/bric/eval.py:50-51`; existing train-helper imports `src/bric/eval.py:14-16`; roots checked: documented `bric.train` and runnable `bric.eval` entry points, CI and tests | delete the eval copy and call the existing train helper | confidence high

WP6-10 | S | `src/api/bric_inference.py:classify` | API RGB normalisation repeats the exact dataset constants and formula, so the API can absorb the constants from `bric.dataset` without changing its in-memory clip path. | evidence: shared constants and dataset formula `src/bric/dataset.py:38-40,211-216`; duplicated API literals and formula `src/api/bric_inference.py:486-496`; existing BRIC dataset import `src/api/bric_inference.py:33-35`; roots checked: Docker BRIC route, training dataset entry points, tests and imports | delete `_RGB_MEAN_VAL` and `_RGB_STD_VAL` and reuse `_RGB_MEAN` and `_RGB_STD`, leaving the local transpose loop | confidence high

WP6-11 | U | `src/api/bric_inference.py:_ensure_initialised` | The BRIC API’s court deployment knob is not end-to-end because it does not read `court_encoder` and never supplies the three court tensors required by `BRICNetwork`. | evidence: API manifest handling and network construction `src/api/bric_inference.py:173-189`; required court inputs `src/bric/network.py:268-329`; API forward kwargs `src/api/bric_inference.py:498-510`; current deployed manifest has `use_court: false` at `runtime/deployed/bric/*/manifest.yaml:3-7,49-51`; training variants still expose court at `src/bric/train.py:41-45`; roots checked: Docker BRIC route, BRIC train/eval entry points, deployed manifest and dynamic configuration | remove court-enabled deployment handling until the API supplies the required inputs, retaining court variants in training and evaluation | confidence med

WP6-12 | C | `src/bric/preprocessing/{slice_rallies,extract_shuttle,preprocess_videos}.find_source_video` | The preprocessing modules intentionally mirror source-video lookup, while slice and shuttle extraction mirror rally-bound derivation with different return shapes for independently runnable stages. | evidence: lookup and bounds `src/bric/preprocessing/slice_rallies.py:56-70`, `src/bric/preprocessing/extract_shuttle.py:55-72`, `src/bric/preprocessing/preprocess_videos.py:97-104`; separate CLI entry points `src/bric/preprocessing/slice_rallies.py:191-231`, `src/bric/preprocessing/extract_shuttle.py:182-217`, `src/bric/preprocessing/preprocess_videos.py:521-569`; roots checked: documented preprocessing modules, CI scripts and `__main__` roots | leave the isolation mirrors separate because centralising them would couple independent CLI stages and their output shapes | confidence high

WP6-13 | C | `src/bric/perception/shuttle.py:extract_shuttle` | BRIC preprocessing and API inference both use TrackNet through deliberate isolation paths, with preprocessing invoking a vendor subprocess and API running an in-process job path. | evidence: subprocess rationale and wrapper `src/bric/perception/shuttle.py:1-11,48-105`; preprocessing CSV and dense-cache path `src/bric/preprocessing/extract_shuttle.py:121-165`; API vendor imports and live TrackNet path `src/api/bric_inference.py:48-55,415-482`; roots checked: documented preprocessing entry point, Docker upload route and dynamic imports | leave separate because the CLI/temp-CSV path and in-memory uploaded-video path prevent vendor import and lifecycle coupling | confidence high

WP6-14 | C | `src/api/{inference,bric_inference,bst_x_inference}.py` | The inference triplet shares a prediction purpose but is not supersedable because the stub, BRIC upload path and BST library path have different inputs, preparation, side effects and return contracts. | evidence: stub behaviour `src/api/inference.py:1-14,33-50,177-225`; BRIC path `src/api/bric_inference.py:227-243,249-381,415-572`; BST path `src/api/bst_x_inference.py:1-21,183-272`; dispatcher `src/api/main.py:208-301`; roots checked: Docker upload/library routes, registry routes, tests and dynamic imports | leave as explicit dispatch paths because absorbing one into another would change the route contracts and supported input types | confidence high

WP6-15 | C | `src/bric/dataset.py:ShuttleSetDataset` vs `src/shared/dataset.py` | `bric.dataset` and `shared.dataset` are different layers, with BRIC owning cache/tensor assembly and shared owning paths, curation, splits and temporal utilities. | evidence: BRIC dataset layer `src/bric/dataset.py:1-23,92-150,211-240,242-335`; shared paths and curation `src/shared/dataset.py:1-47,63-80,86-139`; shared temporal utilities `src/shared/dataset.py:217-277`; roots checked: BRIC training entry points, API imports, CI and documented pipeline roots | leave separate because merging them would make shared data utilities depend on BRIC tensor and model concerns | confidence high

WP6-16 | C | `src/bric/dataset.py:_build_court_snapshot` vs `src/api/bric_inference.py:_striker_court_position` | Offline and online court projection use equivalent geometry but consume different state, so neither path can absorb the other without coupling API inference to training caches. | evidence: offline snapshot smoothing and projection `src/bric/dataset.py:242-296`; offline sequence path `src/bric/dataset.py:298-335`; online smoothing and projection `src/api/bric_inference.py:115-158`; online arrays and response use `src/api/bric_inference.py:325-350,524-528`; roots checked: BRIC dataset/training roots, Docker BRIC route and tests | leave separate because offline NPZ caches and live per-upload arrays are different lifecycle boundaries | confidence high

OUTWARD NOTES:

`src/bric/perception/shuttle.py:35-45` and `src/bst_x/pipeline/shuttle_extractor.py:33-41,83-109` duplicate TrackNet stride/evaluation settings and differ on `large_video`; route the cross-package comparison to WP2.

`src/bric/preprocessing/slice_rallies.py:95-99` and `src/bst_x/pipeline/clip_generator.py:94` both apply the half-frame seek offset; verify frame-seek equivalence in WP2.

`src/shared/dataset.py:207-277`, `src/api/bric_inference.py:39,271` and `src/bst_x/pipeline/clip_generator.py` form a cross-package clip-window mirror; WP2 owns the taxonomy and coupling decision.

`docs/api_contract.md:120-125,215-217` advertises `match` query filters, but `src/api/registry.py:399-434` exposes no `match` filter; check the API/frontend contract outside WP6.

NOT CHECKED: `src/bric/perception/_vendor` and `src/bric/diagnostics` were not line-audited because they belong to other work packages; diagnostic readers were checked only to verify cache-field usage. No runtime inference, preprocessing, TrackNet or full pytest run was performed because this was a read-only audit. Scoped Pyrefly exited 0 and scoped Ruff F401/F841 exited 0; full Ruff could not create its cache in the read-only filesystem. `src/bric/eval.py` was treated as runnable but undocumented, not automatically dead. No P or O finding met the evidence bar.

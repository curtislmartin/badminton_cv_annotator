# Pinned dependency set for CI (single source of truth: uv.lock).
#
# Pins are taken from the uv lockfile. After changing dependencies in
# pyproject.toml (and `uv lock`), update them with:
#   ./scripts/gen-requirements.sh --check     # reports any pin that drifted from uv.lock
# then bump the versions below to match. See docs/ci.md.
#
# torch / torchvision are intentionally LEFT UNPINNED here. CI installs their
# CPU builds first from the PyTorch index, so pip sees them as already satisfied.
# Pinning them here would fight that step because the lockfile resolves torch to
# a `+cu128` build on Linux.

# --- ML & Data ---
torch                 # installed via explicit --index-url before this file; unpinned on purpose
torchvision           # ditto
numpy==2.4.4
pandas==2.3.3
matplotlib==3.10.9
scikit-learn==1.8.0
opencv-python==5.0.0.93      # cv2; classifier video metadata and video tests
ultralytics==8.4.50          # YOLO11 + ByteTrack; perception/players.py + scripts/preprocess_videos.py
positional-encodings==6.0.4  # Positional encoding layers used by BST-X transformer model (bst_x/model/)
torchinfo==1.8.0             # Model summary utility used by tempose.py in the BST-X model module
tensorboard==2.20.0          # Backs torch.utils.tensorboard.SummaryWriter (imported at module load in bst_x_train.py)
torcheval==0.0.7             # multiclass_f1_score used in the bst_x_train training loop
transformers==5.8.1          # HuggingFace; provides get_cosine_schedule_with_warmup for the bst_x_train LR schedule
httpx==0.28.1                # Indirect transport dependency of transformers via huggingface-hub
pyyaml==6.0.3                # tests/test_active_classes.py reads taxonomy YAMLs
parse==1.22.0                # TrackNetV3 vendor dep (src/shared/tracknetv3)

# --- Testing & Infrastructure ---
pytest==9.0.3

# bst_x subproject has complex dependencies; so requires 2-3 separate venvs depending on how you run it.
# No longer requires Python <=3.11 since pose moved to RTMLib.

jaxtyping
beartype
frozendict>=2.4,<3           # Hyp's immutable dict defaults (bst_x_train); matches the pyproject pin

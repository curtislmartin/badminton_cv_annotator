# Optional local commit-message rules, run through pre-commit.
# Docs: https://jorisroovers.com/gitlint/latest/configuration/
#
# Goal: catch placeholder messages without enforcing length or format.

[general]
# Don't force a body on small commits.
ignore=body-is-missing

[title-must-not-contain-word]
# Reject low-signal placeholder subjects.
words=wip,fixup,asdf,test123,stuff,temp

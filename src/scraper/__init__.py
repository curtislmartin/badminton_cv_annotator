"""Commentary-scraper components for the scrape-to-dataset pipeline.

The config module is the single source of truth for file contracts and named
constants. See ``docs/scraper_pipeline/scraper_architecture.md`` for the current
stage and file map. Each responsibility-based module is runnable as
``python -m scraper.<module>`` with ``PYTHONPATH=src``.
"""
